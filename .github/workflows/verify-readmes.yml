# Ensures README files are reflective of the current repository structure.
name: Verify READMEs

on:
  pull_request:
    branches: [main, master]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run generate_misc.sh
        run: bash generate_misc.sh

      - name: Check for changes
        run: |
          if [[ -n $(git status --porcelain) ]]; then
            echo "❌ README files are out of date!"
            echo ""
            echo "The following files have changed:"
            git status --porcelain
            echo ""
            echo "Please run 'bash generate_misc.sh' locally and commit the changes."
            exit 1
          else
            echo "✅ All README files are up to date!"
          fi

